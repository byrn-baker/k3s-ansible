---
# ansible-playbook install-gitlab.yml --tags "staging-ingress"
# ansible-playbook install-gitlab.yml --tags "production-install"
- name: Install Gitlab on k3s
  hosts: localhost
  roles:
    - install-gitlab